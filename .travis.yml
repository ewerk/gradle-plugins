sudo: false
language: android

android:
  components:
    - tools
    - build-tools-21.1.2
    - android-21
    - platform-tools  

jdk:
  - oraclejdk8

script: ./gradlew build --stacktrace -i

cache:
  directories:
    - "$HOME/.gradle/caches"

branches:
  only:
    - master
