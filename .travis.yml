sudo: false
language: android

android:
  components:
    - tools
    - platform-tools  
    - build-tools-27.0.1
    - android-25
  licenses:
    - 'android-sdk-license-.*'     

jdk:
  - oraclejdk8

script: ./gradlew build --stacktrace -i

cache:
  directories:
    - "$HOME/.gradle/caches"

branches:
  only:
    - master
